name: biliscope-mcp
version: 2.1.3
description: Bilibili MCP with CookieCloud auto refresh and single JSON deployment

runtime: node

transport: stdio

entry: src/index.ts

build:
  output: dist/index.js
